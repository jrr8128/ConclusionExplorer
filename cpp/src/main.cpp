#include <iostream>

int main() {
  std::cout << "It works!" << std::endl;
  return 0;
}

// canon canon_instance{};

// memo instantiation
// memo memo_instance{canon_instance};
// memo_instance.seen.reserve(200000);
// memo_instance.seen.max_load_factor(0.7f);